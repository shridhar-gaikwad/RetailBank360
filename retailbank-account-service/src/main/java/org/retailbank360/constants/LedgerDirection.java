package org.retailbank360.constants;

/** Which way money moved on one account. */
public enum LedgerDirection {

    /** Money left the account. */
    DEBIT,

    /** Money arrived in the account. */
    CREDIT
}
