server:
  port: 8086

spring:
  application:
    name: retailbank-notification-service

  datasource:
    url: jdbc:postgresql://localhost:5432/retailbank_notification_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.hibernate.SQL: DEBUG