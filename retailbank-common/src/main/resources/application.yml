server:
  port: 8083

spring:
  application:
    name: retailbank-common-service

  datasource:
    url: jdbc:postgresql://localhost:5432/retailbank_common_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    org.hibernate.SQL: DEBUG